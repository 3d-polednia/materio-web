# Node / tooling
node_modules/
npm-debug.log*
.DS_Store
Thumbs.db
# Never commit secrets
.env
.env.*
*.jks
local.properties

# Cloud Functions build output — the source is committed, the install is not
functions/node_modules/
